[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tamfunc"               
version = "0.1.0"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
  { name = "Yukito Tamura" }
]

dependencies = [
  "numpy",
  "xarray",
  "cftime",
  "scipy",
  "dask",
]

[project.urls]
Homepage = "https://github.com/y-tamura/tamfunc"